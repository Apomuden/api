before_script:
  - apt-get update -qq
  - apt-get install -qq git
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh

stages:
  - deploy

deploy:
  stage: deploy
  only:
    - 15-ci-cd-should-update-the-server
  script:
    - ssh willie@78.141.192.241 "cd /var/www/html/ssisgh/test-api && git pull origin dev
